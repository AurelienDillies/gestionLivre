{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!
{% endblock %}

{% block body %}

<header>
		<a href="{{ path('app_home') }}">Acceuil</a>
        <a href="{{ path('app_create') }}">Création de livres</a>
	</header>

	<div>
			<p>
				Titre :
				{{ book.titre }}
			</p>
			<p>
				Résumer :
				{{ book.resumer }}
			</p>
			<p>
				Description :
				{{ book.description }}
			</p>
			<p>
				Prix :
				{{ book.prix }}
				€
			</p>
			<p>
				Id :
				{{book.id}}
			</p>

	</div>
{% endblock %}
